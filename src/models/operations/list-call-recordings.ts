/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

export type ListCallRecordingsRequest = {
  meetingId: string;
  limit?: number | undefined;
  cursor?: string | undefined;
};

export type ListCallRecordingsId = {
  /**
   * The ID of the workspace this call recording belongs to.
   */
  workspaceId: string;
  /**
   * The ID of the meeting associated with this call recording.
   */
  meetingId: string;
  /**
   * The call recording ID of the call recording.
   */
  callRecordingId: string;
};

/**
 * The status of the call recording. When a call recording is first created, it will have a status of `PROCESSING`. Once the recording is ready, it will transition to `COMPLETED`. If the recording fails for any reason, the status will be `FAILED`.
 */
export const ListCallRecordingsStatus = {
  Processing: "processing",
  Completed: "completed",
  Failed: "failed",
} as const;
/**
 * The status of the call recording. When a call recording is first created, it will have a status of `PROCESSING`. Once the recording is ready, it will transition to `COMPLETED`. If the recording fails for any reason, the status will be `FAILED`.
 */
export type ListCallRecordingsStatus = OpenEnum<
  typeof ListCallRecordingsStatus
>;

/**
 * The type of actor. [Read more information on actor types here](/docs/actors).
 */
export const ListCallRecordingsType = {
  ApiToken: "api-token",
  WorkspaceMember: "workspace-member",
  System: "system",
  App: "app",
} as const;
/**
 * The type of actor. [Read more information on actor types here](/docs/actors).
 */
export type ListCallRecordingsType = OpenEnum<typeof ListCallRecordingsType>;

/**
 * The actor that created this call recording.
 */
export type ListCallRecordingsCreatedByActor = {
  /**
   * An ID to identify the actor.
   */
  id?: string | null | undefined;
  /**
   * The type of actor. [Read more information on actor types here](/docs/actors).
   */
  type?: ListCallRecordingsType | null | undefined;
};

export type ListCallRecordingsData = {
  id: ListCallRecordingsId;
  /**
   * The status of the call recording. When a call recording is first created, it will have a status of `PROCESSING`. Once the recording is ready, it will transition to `COMPLETED`. If the recording fails for any reason, the status will be `FAILED`.
   */
  status: ListCallRecordingsStatus;
  /**
   * A URL that links directly to the call recording in the Attio web application.
   */
  webUrl: string;
  /**
   * The actor that created this call recording.
   */
  createdByActor: ListCallRecordingsCreatedByActor;
  /**
   * The timestamp of when the call recording was created.
   */
  createdAt: string;
};

export type ListCallRecordingsPagination = {
  nextCursor: string | null;
};

/**
 * Success
 */
export type ListCallRecordingsResponse = {
  data: Array<ListCallRecordingsData>;
  pagination: ListCallRecordingsPagination;
};

/** @internal */
export type ListCallRecordingsRequest$Outbound = {
  meeting_id: string;
  limit?: number | undefined;
  cursor?: string | undefined;
};

/** @internal */
export const ListCallRecordingsRequest$outboundSchema: z.ZodMiniType<
  ListCallRecordingsRequest$Outbound,
  ListCallRecordingsRequest
> = z.pipe(
  z.object({
    meetingId: z.string(),
    limit: z.optional(z.int()),
    cursor: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      meetingId: "meeting_id",
    });
  }),
);

export function listCallRecordingsRequestToJSON(
  listCallRecordingsRequest: ListCallRecordingsRequest,
): string {
  return JSON.stringify(
    ListCallRecordingsRequest$outboundSchema.parse(listCallRecordingsRequest),
  );
}

/** @internal */
export const ListCallRecordingsId$inboundSchema: z.ZodMiniType<
  ListCallRecordingsId,
  unknown
> = z.pipe(
  z.object({
    workspace_id: types.string(),
    meeting_id: types.string(),
    call_recording_id: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "workspace_id": "workspaceId",
      "meeting_id": "meetingId",
      "call_recording_id": "callRecordingId",
    });
  }),
);

export function listCallRecordingsIdFromJSON(
  jsonString: string,
): SafeParseResult<ListCallRecordingsId, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListCallRecordingsId$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListCallRecordingsId' from JSON`,
  );
}

/** @internal */
export const ListCallRecordingsStatus$inboundSchema: z.ZodMiniType<
  ListCallRecordingsStatus,
  unknown
> = openEnums.inboundSchema(ListCallRecordingsStatus);

/** @internal */
export const ListCallRecordingsType$inboundSchema: z.ZodMiniType<
  ListCallRecordingsType,
  unknown
> = openEnums.inboundSchema(ListCallRecordingsType);

/** @internal */
export const ListCallRecordingsCreatedByActor$inboundSchema: z.ZodMiniType<
  ListCallRecordingsCreatedByActor,
  unknown
> = z.object({
  id: z.optional(z.nullable(types.string())),
  type: z.optional(z.nullable(ListCallRecordingsType$inboundSchema)),
});

export function listCallRecordingsCreatedByActorFromJSON(
  jsonString: string,
): SafeParseResult<ListCallRecordingsCreatedByActor, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListCallRecordingsCreatedByActor$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListCallRecordingsCreatedByActor' from JSON`,
  );
}

/** @internal */
export const ListCallRecordingsData$inboundSchema: z.ZodMiniType<
  ListCallRecordingsData,
  unknown
> = z.pipe(
  z.object({
    id: z.lazy(() => ListCallRecordingsId$inboundSchema),
    status: ListCallRecordingsStatus$inboundSchema,
    web_url: types.string(),
    created_by_actor: z.lazy(() =>
      ListCallRecordingsCreatedByActor$inboundSchema
    ),
    created_at: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "web_url": "webUrl",
      "created_by_actor": "createdByActor",
      "created_at": "createdAt",
    });
  }),
);

export function listCallRecordingsDataFromJSON(
  jsonString: string,
): SafeParseResult<ListCallRecordingsData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListCallRecordingsData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListCallRecordingsData' from JSON`,
  );
}

/** @internal */
export const ListCallRecordingsPagination$inboundSchema: z.ZodMiniType<
  ListCallRecordingsPagination,
  unknown
> = z.pipe(
  z.object({
    next_cursor: types.nullable(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "next_cursor": "nextCursor",
    });
  }),
);

export function listCallRecordingsPaginationFromJSON(
  jsonString: string,
): SafeParseResult<ListCallRecordingsPagination, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListCallRecordingsPagination$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListCallRecordingsPagination' from JSON`,
  );
}

/** @internal */
export const ListCallRecordingsResponse$inboundSchema: z.ZodMiniType<
  ListCallRecordingsResponse,
  unknown
> = z.object({
  data: z.array(z.lazy(() => ListCallRecordingsData$inboundSchema)),
  pagination: z.lazy(() => ListCallRecordingsPagination$inboundSchema),
});

export function listCallRecordingsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListCallRecordingsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListCallRecordingsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListCallRecordingsResponse' from JSON`,
  );
}
