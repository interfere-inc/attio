/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import * as models from "../index.js";

/**
 * Specify the format for the note's content. Choose from:
 *
 * @remarks
 * - `plaintext`: Standard text format where `\n` signifies a new line.
 * - `markdown`: Enables rich text formatting using a subset of Markdown syntax:
 *   - **Headings**: Levels 1-3 (`#`, `##`, `###`).
 *   - **Lists**: Unordered (`-`, `*`, `+`) and ordered (`1.`, `2.`).
 *   - **Text styles**: Bold (`**bold**` or `__bold__`), italic (`*italic*` or `_italic_`), strikethrough (`~~strikethrough~~`), and highlight (`==highlighted==`).
 *   - **Links**: Standard Markdown links (`[link text](https://example.com)`).
 *
 *   *Note: While the Attio interface supports image embeds, they cannot currently be added or retrieved via the API's markdown format.*
 */
export const PatchV2NotesNoteIdFormat = {
  Plaintext: "plaintext",
  Markdown: "markdown",
} as const;
/**
 * Specify the format for the note's content. Choose from:
 *
 * @remarks
 * - `plaintext`: Standard text format where `\n` signifies a new line.
 * - `markdown`: Enables rich text formatting using a subset of Markdown syntax:
 *   - **Headings**: Levels 1-3 (`#`, `##`, `###`).
 *   - **Lists**: Unordered (`-`, `*`, `+`) and ordered (`1.`, `2.`).
 *   - **Text styles**: Bold (`**bold**` or `__bold__`), italic (`*italic*` or `_italic_`), strikethrough (`~~strikethrough~~`), and highlight (`==highlighted==`).
 *   - **Links**: Standard Markdown links (`[link text](https://example.com)`).
 *
 *   *Note: While the Attio interface supports image embeds, they cannot currently be added or retrieved via the API's markdown format.*
 */
export type PatchV2NotesNoteIdFormat = ClosedEnum<
  typeof PatchV2NotesNoteIdFormat
>;

export type PatchV2NotesNoteIdData = {
  /**
   * The note title. The title is plaintext only and has no formatting.
   */
  title?: string | undefined;
  /**
   * Specify the format for the note's content. Choose from:
   *
   * @remarks
   * - `plaintext`: Standard text format where `\n` signifies a new line.
   * - `markdown`: Enables rich text formatting using a subset of Markdown syntax:
   *   - **Headings**: Levels 1-3 (`#`, `##`, `###`).
   *   - **Lists**: Unordered (`-`, `*`, `+`) and ordered (`1.`, `2.`).
   *   - **Text styles**: Bold (`**bold**` or `__bold__`), italic (`*italic*` or `_italic_`), strikethrough (`~~strikethrough~~`), and highlight (`==highlighted==`).
   *   - **Links**: Standard Markdown links (`[link text](https://example.com)`).
   *
   *   *Note: While the Attio interface supports image embeds, they cannot currently be added or retrieved via the API's markdown format.*
   */
  format?: PatchV2NotesNoteIdFormat | undefined;
  /**
   * The main content of the note, formatted according to the value provided in the `format` field. Use `\n` for line breaks in `plaintext`. For `markdown`, utilize the supported syntax elements to structure and style your note.
   */
  content?: string | undefined;
};

export type PatchV2NotesNoteIdRequestBody = {
  data: PatchV2NotesNoteIdData;
};

export type PatchV2NotesNoteIdRequest = {
  noteId: string;
  body: PatchV2NotesNoteIdRequestBody;
};

/**
 * Success
 */
export type PatchV2NotesNoteIdResponse = {
  data: models.Note;
};

/** @internal */
export const PatchV2NotesNoteIdFormat$outboundSchema: z.ZodMiniEnum<
  typeof PatchV2NotesNoteIdFormat
> = z.enum(PatchV2NotesNoteIdFormat);

/** @internal */
export type PatchV2NotesNoteIdData$Outbound = {
  title?: string | undefined;
  format?: string | undefined;
  content?: string | undefined;
};

/** @internal */
export const PatchV2NotesNoteIdData$outboundSchema: z.ZodMiniType<
  PatchV2NotesNoteIdData$Outbound,
  PatchV2NotesNoteIdData
> = z.object({
  title: z.optional(z.string()),
  format: z.optional(PatchV2NotesNoteIdFormat$outboundSchema),
  content: z.optional(z.string()),
});

export function patchV2NotesNoteIdDataToJSON(
  patchV2NotesNoteIdData: PatchV2NotesNoteIdData,
): string {
  return JSON.stringify(
    PatchV2NotesNoteIdData$outboundSchema.parse(patchV2NotesNoteIdData),
  );
}

/** @internal */
export type PatchV2NotesNoteIdRequestBody$Outbound = {
  data: PatchV2NotesNoteIdData$Outbound;
};

/** @internal */
export const PatchV2NotesNoteIdRequestBody$outboundSchema: z.ZodMiniType<
  PatchV2NotesNoteIdRequestBody$Outbound,
  PatchV2NotesNoteIdRequestBody
> = z.object({
  data: z.lazy(() => PatchV2NotesNoteIdData$outboundSchema),
});

export function patchV2NotesNoteIdRequestBodyToJSON(
  patchV2NotesNoteIdRequestBody: PatchV2NotesNoteIdRequestBody,
): string {
  return JSON.stringify(
    PatchV2NotesNoteIdRequestBody$outboundSchema.parse(
      patchV2NotesNoteIdRequestBody,
    ),
  );
}

/** @internal */
export type PatchV2NotesNoteIdRequest$Outbound = {
  note_id: string;
  body: PatchV2NotesNoteIdRequestBody$Outbound;
};

/** @internal */
export const PatchV2NotesNoteIdRequest$outboundSchema: z.ZodMiniType<
  PatchV2NotesNoteIdRequest$Outbound,
  PatchV2NotesNoteIdRequest
> = z.pipe(
  z.object({
    noteId: z.string(),
    body: z.lazy(() => PatchV2NotesNoteIdRequestBody$outboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      noteId: "note_id",
    });
  }),
);

export function patchV2NotesNoteIdRequestToJSON(
  patchV2NotesNoteIdRequest: PatchV2NotesNoteIdRequest,
): string {
  return JSON.stringify(
    PatchV2NotesNoteIdRequest$outboundSchema.parse(patchV2NotesNoteIdRequest),
  );
}

/** @internal */
export const PatchV2NotesNoteIdResponse$inboundSchema: z.ZodMiniType<
  PatchV2NotesNoteIdResponse,
  unknown
> = z.object({
  data: models.Note$inboundSchema,
});

export function patchV2NotesNoteIdResponseFromJSON(
  jsonString: string,
): SafeParseResult<PatchV2NotesNoteIdResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PatchV2NotesNoteIdResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PatchV2NotesNoteIdResponse' from JSON`,
  );
}
