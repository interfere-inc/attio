/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type ObjectViewId = {
  /**
   * A UUID identifying the workspace this view belongs to.
   */
  workspaceId: string;
  /**
   * A UUID identifying the object this view belongs to.
   */
  objectId: string;
  /**
   * A UUID identifying this view. Use this value as `filter_view_id` when listing records.
   */
  viewId: string;
};

export type ObjectView = {
  /**
   * The title of the view shown in the Attio app.
   */
  title: string;
  id: ObjectViewId;
  /**
   * When the view was created.
   */
  createdAt: string;
};

/** @internal */
export const ObjectViewId$inboundSchema: z.ZodMiniType<ObjectViewId, unknown> =
  z.pipe(
    z.object({
      workspace_id: types.string(),
      object_id: types.string(),
      view_id: types.string(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "workspace_id": "workspaceId",
        "object_id": "objectId",
        "view_id": "viewId",
      });
    }),
  );

export function objectViewIdFromJSON(
  jsonString: string,
): SafeParseResult<ObjectViewId, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ObjectViewId$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ObjectViewId' from JSON`,
  );
}

/** @internal */
export const ObjectView$inboundSchema: z.ZodMiniType<ObjectView, unknown> = z
  .pipe(
    z.object({
      title: types.string(),
      id: z.lazy(() => ObjectViewId$inboundSchema),
      created_at: types.string(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "created_at": "createdAt",
      });
    }),
  );

export function objectViewFromJSON(
  jsonString: string,
): SafeParseResult<ObjectView, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ObjectView$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ObjectView' from JSON`,
  );
}
