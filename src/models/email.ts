/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type EmailId = {
  /**
   * The ID of the workspace the email belongs to.
   */
  workspaceId: string;
  /**
   * The ID of the mailbox this copy of the email was read from. A single email sent to several people in your workspace is stored once per mailbox; this endpoint returns one entry per email, so the returned `mailbox_id` identifies whichever copy was readable.
   */
  mailboxId: string;
  /**
   * The ID of the Attio email.
   */
  emailId: string;
};

/**
 * Whether the email was sent from your workspace or received by it.
 */
export const Direction = {
  Inbound: "inbound",
  Outbound: "outbound",
} as const;
/**
 * Whether the email was sent from your workspace or received by it.
 */
export type Direction = OpenEnum<typeof Direction>;

/**
 * The role this participant had on the email.
 */
export const Role = {
  From: "from",
  ReplyTo: "reply-to",
  To: "to",
  Cc: "cc",
  Bcc: "bcc",
} as const;
/**
 * The role this participant had on the email.
 */
export type Role = OpenEnum<typeof Role>;

export type EmailParticipant = {
  /**
   * The participant's name as it appeared on the email, when the email provided one.
   */
  name: string | null;
  /**
   * The role this participant had on the email.
   */
  role: Role;
  /**
   * The normalized email address of the participant.
   */
  emailAddress: string;
  /**
   * The domain of the participant's email address.
   */
  emailDomain: string;
};

export type EmailLinkedRecord = {
  /**
   * The slug of the object the linked record belongs to.
   */
  objectSlug: string;
  /**
   * The ID of the object the linked record belongs to.
   */
  objectId: string;
  /**
   * The ID of the linked record.
   */
  recordId: string;
};

export type Email = {
  id: EmailId;
  /**
   * Timestamp representing when the email was sent, taken from the email's own headers rather than from when Attio imported it.
   */
  sentAt: string;
  /**
   * Whether the email was sent from your workspace or received by it.
   */
  direction: Direction;
  /**
   * The subject line of the email. This is `null` when the email has no subject or is from a mailbox shared as metadata only.
   */
  subjectLine: string | null;
  /**
   * The participants on the email. Note that `bcc` participants are realistically only present on outbound email: inbound messages do not disclose the other recipients' blind copies, so an absent `bcc` participant is not evidence that there was none.
   */
  participants: Array<EmailParticipant>;
  /**
   * The person and company records whose email addresses or domains match this email's participants. Unlike meetings, this link is derived when you make the request rather than stored, so it reflects your records as they are now: creating a person record today will make older emails start reporting it.
   */
  linkedRecords: Array<EmailLinkedRecord>;
};

/** @internal */
export const EmailId$inboundSchema: z.ZodMiniType<EmailId, unknown> = z.pipe(
  z.object({
    workspace_id: types.string(),
    mailbox_id: types.string(),
    email_id: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "workspace_id": "workspaceId",
      "mailbox_id": "mailboxId",
      "email_id": "emailId",
    });
  }),
);

export function emailIdFromJSON(
  jsonString: string,
): SafeParseResult<EmailId, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EmailId$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EmailId' from JSON`,
  );
}

/** @internal */
export const Direction$inboundSchema: z.ZodMiniType<Direction, unknown> =
  openEnums.inboundSchema(Direction);

/** @internal */
export const Role$inboundSchema: z.ZodMiniType<Role, unknown> = openEnums
  .inboundSchema(Role);

/** @internal */
export const EmailParticipant$inboundSchema: z.ZodMiniType<
  EmailParticipant,
  unknown
> = z.pipe(
  z.object({
    name: types.nullable(types.string()),
    role: Role$inboundSchema,
    email_address: types.string(),
    email_domain: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "email_address": "emailAddress",
      "email_domain": "emailDomain",
    });
  }),
);

export function emailParticipantFromJSON(
  jsonString: string,
): SafeParseResult<EmailParticipant, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EmailParticipant$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EmailParticipant' from JSON`,
  );
}

/** @internal */
export const EmailLinkedRecord$inboundSchema: z.ZodMiniType<
  EmailLinkedRecord,
  unknown
> = z.pipe(
  z.object({
    object_slug: types.string(),
    object_id: types.string(),
    record_id: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "object_slug": "objectSlug",
      "object_id": "objectId",
      "record_id": "recordId",
    });
  }),
);

export function emailLinkedRecordFromJSON(
  jsonString: string,
): SafeParseResult<EmailLinkedRecord, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EmailLinkedRecord$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EmailLinkedRecord' from JSON`,
  );
}

/** @internal */
export const Email$inboundSchema: z.ZodMiniType<Email, unknown> = z.pipe(
  z.object({
    id: z.lazy(() => EmailId$inboundSchema),
    sent_at: types.string(),
    direction: Direction$inboundSchema,
    subject_line: types.nullable(types.string()),
    participants: z.array(z.lazy(() => EmailParticipant$inboundSchema)),
    linked_records: z.array(z.lazy(() => EmailLinkedRecord$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "sent_at": "sentAt",
      "subject_line": "subjectLine",
      "linked_records": "linkedRecords",
    });
  }),
);

export function emailFromJSON(
  jsonString: string,
): SafeParseResult<Email, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Email$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Email' from JSON`,
  );
}
