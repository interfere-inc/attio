/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type ListViewId = {
  /**
   * A UUID identifying the workspace this view belongs to.
   */
  workspaceId: string;
  /**
   * A UUID identifying the list this view belongs to.
   */
  listId: string;
  /**
   * A UUID identifying this view. Use this value as `filter_view_id` when listing entries.
   */
  viewId: string;
};

export type ListView = {
  id: ListViewId;
  /**
   * The title of the view shown in the Attio app.
   */
  title: string;
  /**
   * When the view was created.
   */
  createdAt: string;
};

/** @internal */
export const ListViewId$inboundSchema: z.ZodMiniType<ListViewId, unknown> = z
  .pipe(
    z.object({
      workspace_id: types.string(),
      list_id: types.string(),
      view_id: types.string(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "workspace_id": "workspaceId",
        "list_id": "listId",
        "view_id": "viewId",
      });
    }),
  );

export function listViewIdFromJSON(
  jsonString: string,
): SafeParseResult<ListViewId, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListViewId$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListViewId' from JSON`,
  );
}

/** @internal */
export const ListView$inboundSchema: z.ZodMiniType<ListView, unknown> = z.pipe(
  z.object({
    id: z.lazy(() => ListViewId$inboundSchema),
    title: types.string(),
    created_at: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
    });
  }),
);

export function listViewFromJSON(
  jsonString: string,
): SafeParseResult<ListView, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListView$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListView' from JSON`,
  );
}
