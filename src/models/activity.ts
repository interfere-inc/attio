/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type ActivityId = {
  /**
   * A UUID to identify the workspace this activity belongs to.
   */
  workspaceId: string;
  /**
   * A UUID to identify the activity.
   */
  activityId: string;
};

export type Extend = {
  /**
   * A UUID to identify the schema.
   */
  schemaId: string;
  /**
   * The human-readable slug of the schema.
   */
  schemaSlug: string;
};

export type Activity = {
  id: ActivityId;
  /**
   * A unique, human-readable slug to access the activity through URLs and API calls. Formatted in snake case.
   */
  apiSlug: string;
  /**
   * The singular form of the activity's name.
   */
  singularNoun: string;
  /**
   * The plural form of the activity's name.
   */
  pluralNoun: string;
  /**
   * The schemas this activity directly extends, which supply its inherited attributes.
   */
  extends: Array<Extend>;
  /**
   * `true` when Attio defines the activity, `false` when it's a custom activity.
   */
  isSystemActivity: boolean;
  /**
   * When the activity was created.
   */
  createdAt: string;
};

/** @internal */
export const ActivityId$inboundSchema: z.ZodMiniType<ActivityId, unknown> = z
  .pipe(
    z.object({
      workspace_id: types.string(),
      activity_id: types.string(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "workspace_id": "workspaceId",
        "activity_id": "activityId",
      });
    }),
  );

export function activityIdFromJSON(
  jsonString: string,
): SafeParseResult<ActivityId, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ActivityId$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ActivityId' from JSON`,
  );
}

/** @internal */
export const Extend$inboundSchema: z.ZodMiniType<Extend, unknown> = z.pipe(
  z.object({
    schema_id: types.string(),
    schema_slug: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "schema_id": "schemaId",
      "schema_slug": "schemaSlug",
    });
  }),
);

export function extendFromJSON(
  jsonString: string,
): SafeParseResult<Extend, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Extend$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Extend' from JSON`,
  );
}

/** @internal */
export const Activity$inboundSchema: z.ZodMiniType<Activity, unknown> = z.pipe(
  z.object({
    id: z.lazy(() => ActivityId$inboundSchema),
    api_slug: types.string(),
    singular_noun: types.string(),
    plural_noun: types.string(),
    extends: z.array(z.lazy(() => Extend$inboundSchema)),
    is_system_activity: types.boolean(),
    created_at: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "api_slug": "apiSlug",
      "singular_noun": "singularNoun",
      "plural_noun": "pluralNoun",
      "is_system_activity": "isSystemActivity",
      "created_at": "createdAt",
    });
  }),
);

export function activityFromJSON(
  jsonString: string,
): SafeParseResult<Activity, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Activity$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Activity' from JSON`,
  );
}
