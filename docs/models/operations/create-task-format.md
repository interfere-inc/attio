# CreateTaskFormat

The format of the task content to be created. Rich text formatting, links and @references are not supported.

## Example Usage

```typescript
import { CreateTaskFormat } from "@interfere/attio/models/operations";

let value: CreateTaskFormat = "plaintext";
```

## Values

```typescript
"plaintext"
```